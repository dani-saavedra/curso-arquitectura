apiVersion: apps/v1
kind: Deployment
metadata:
  name: curso
  labels:
    app: curso
spec:
  replicas: 2
  selector:
    matchLabels:
      app: curso
  template:
    metadata:
      labels:
        app: curso
    spec:
      containers:
        - name: curso
          image: danielsabana/curso:1.3.0
          ports:
            - containerPort: 8080
          env:
            - name: NOMBRE_POD
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: 100m
              memory: 500Mi
            limits:
              cpu: 450m
          startupProbe:
            httpGet:
              port: 8080
              path: /saludo
            initialDelaySeconds: 30
